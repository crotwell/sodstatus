import { formatIsoDate } from '../../../helpers/format-iso-date';
import { module, test } from 'qunit';

module('Unit | Helper | format iso date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatIsoDate(42);
  assert.ok(result);
});
